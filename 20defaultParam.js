/*

In JavaScript, default parameters allow you to initialize a function parameter with a default value 
in case the caller of the function doesn't provide a value for that parameter.
This feature was introduced in ECMAScript 6 (ES6).

function greet(name = 'Guest') {
  console.log('Hello, ' + name + '!');
}

greet();           // Outputs: Hello, Guest!
greet('John');     // Outputs: Hello, John!

*/

// REST PARAMETER